# Secrets — never commit
credentials.json
token.json
.drive_config_id
.phantom_state.json
osaurus.yaml

# Python
__pycache__/
*.pyc
*.pyo
*.pyd

# Virtual environment
.venv/

# Build / packaging artifacts
dist/
*.spec
# Exclude pyinstaller/setuptools output dirs but not our build/ scripts dir
/build/*.egg-info
/build/lib
/build/bdist.*

# macOS
.DS_Store
