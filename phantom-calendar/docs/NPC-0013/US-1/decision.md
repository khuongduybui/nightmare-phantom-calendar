- 2026-05-17T00:00:00Z — Decision: Initial state files created for US-1 (extend calendar readers to return title and description). Rationale: Planner phase complete. Impact: phantom-calendar/calendar_reader.py; phantom-calendar/tests/test_calendar_reader.py.- 2026-05-17T05:00:00Z — Decision: Changed personal-event title default from `event.get("summary", "Untitled")` to `event.get("summary") or "Untitled"`. Rationale: Consistency with MSI block handling — both now treat empty-string summaries as "Untitled". Previously, an event with `summary: ""` would keep the empty string as title for personal events but default to "Untitled" for MSI blocks. The new behavior is uniform. Impact: phantom-calendar/calendar_reader.py (get_personal_events).
