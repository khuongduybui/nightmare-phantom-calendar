- 2026-05-17T00:00:00Z — Decision: Initial spec created for NPC-0013 (AI-assisted meeting classification via osaurus). Rationale: feature.md ZeeSpec ↔ AC review passed clean; codebase context confirms calendar readers need title/description plumbing (US-1) before any osaurus integration; osaurus client kept in a separate module (US-2) so it can be unit-tested without touching sync_job. Impact: docs/NPC-0013/spec.md.
- 2026-05-17T00:00:00Z — Decision: Promote `openai` from dev/test dependency to a runtime dependency in requirements.txt. Rationale: this feature uses the openai SDK inside the production sync pipeline (popup-driven classification), not only in test_classify.py. Impact: phantom-calendar/requirements.txt; docs/runes/osaurus.md (rune update flagged in US-2 DoD).
- 2026-05-17T00:00:00Z — Decision: Personal events get the same osaurus suggestion + Recurring/One-shot flow as MSI unknown blocks. Rationale: feature.md AC10 explicitly extends classification to personal events; consistency with MSI flow keeps the user model simple. Impact: phantom-calendar/sync_job.py (new `_classify_personal_events`).
- 2026-05-17T00:00:00Z — Decision: One-shot path recomputes alarm but does not append to `classifications` list returned by popup. Rationale: the existing pipeline writes every entry in `classifications` to Drive config — preserving that contract avoids a second branch in `run_nightly_sync`. Impact: phantom-calendar/sync_job.py.
- 2026-05-17T00:00:00Z — Decision: No retry on osaurus failure; single short timeout (≤3 s) treated as failure. Rationale: feature.md WHEN explicitly forbids retry; UX cost of retries on a slow local server outweighs benefit. Impact: phantom-calendar/osaurus_client.py.
- 2026-05-17T04:40:02Z — Decision: Spec approved by human. Rationale: All user stories reviewed and accepted. spec_hash: 757184e6135b. Impact: docs/NPC-0013/spec.md approved; per-story state files updated.
- 2026-05-17T06:00:00Z — Decision: Read default model from osaurus.yaml `default_module` key instead of hardcoding `"foundation"`. Rationale: config already has the key; avoids hardcoded model names across modules. Fallback remains `"foundation"` when key is absent. Impact: docs/NPC-0013/spec.md (US-2 scope + ACs updated), docs/runes/osaurus.md (schema + model-selection rules updated). Spec re-approved, new hash: ba1561a5443c.
