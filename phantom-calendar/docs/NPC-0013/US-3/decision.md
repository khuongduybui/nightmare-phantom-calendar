- 2026-05-17T00:00:00Z — Decision: Initial state files created for US-3 (wire suggestion + Recurring/One-shot dialogs into sync popup, add personal-event classification). Rationale: Planner phase complete. Impact: phantom-calendar/sync_job.py; phantom-calendar/tests/test_classification_ui.py; phantom-calendar/tests/test_classification_write.py; phantom-calendar/tests/test_sync_job.py.
- 2026-05-17T09:00:00Z — Decision: Pass raw personal events via result["personal_events"] rather than filtering result["all_meetings"]. Rationale: all_meetings candidates lack description field (only computed prep metadata); raw personal events from get_personal_events() have title, description, start needed by _classify_personal_events. Impact: sync_job.run_nightly_sync augments result before _show_popup call.
- 2026-05-17T09:00:00Z — Decision: Moved `from datetime import timedelta` to module-level import per QA LOW4. Pre-existing inline imports in _prompt_unknown_locations (timedelta, datetime as _dt) and _classify_unknown_blocks (resolve_prep_minutes) left unchanged — they are pre-existing code outside the US-3 touch scope and do not affect correctness.
